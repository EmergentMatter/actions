# Owned by the org owner, deliberately alone.
#
# Copied in from EmergentMatter/actions at onboarding. Repo-wide ownership
# routes to @telafifi alone so that every PR in every onboarded repo does
# not auto-request review from every org maintainer -- GitHub requests a
# review from each listed owner on every PR touching an owned path, and
# with `*` that is every PR, in every repo this file is copied into. Add
# other owners here, or scope owners to specific paths, if this repo
# genuinely needs them: sync.py reports the difference rather than
# overwriting it.

*                       @telafifi
